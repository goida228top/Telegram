{
  "name": "rugram-call",
  "version": "1.0.0",
  "description": "Video call application with Mediasoup",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "esbuild index.tsx --bundle --outfile=dist/bundle.js --jsx=automatic --define:process.env.NODE_ENV='\"production\"'",
    "dev": "npm run build && npm start"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mediasoup": "^3.14.4",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "esbuild": "^0.21.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "typescript": "^5.4.5"
  }
}
